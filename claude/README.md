# Low-token lookup layer

Use `catalog.compact.json` for first-pass template discovery. Use `motions.compact.json` only when motion is relevant. These files intentionally omit long descriptions and implementation details.
