import {useMemo,useState} from 'react'
import type {AssetRecord,CreativeBrief,Domain,MasterStoryboard,MasterVisualMap,MediaRecord,MediaRole,OperationalMaster,ProductionMaster,Template,VisualArchitecture} from '../types'
import {buildProductionPlans,productionPrompt} from '../lib/productionPlanner'
import {directionsForMaster} from '../lib/visualDirector'
import {VisualDirectionCard} from './VisualDirectionCard'

export function BriefWorkbench({domains,masters,storyboards,templates,assets,visualMaps,visualArchitectures,productionMasters,mediaRoles,mediaLibrary,onOpen}:{domains:Domain[];masters:OperationalMaster[];storyboards:MasterStoryboard[];templates:Template[];assets:AssetRecord[];visualMaps:MasterVisualMap[];visualArchitectures:VisualArchitecture[];productionMasters:ProductionMaster[];mediaRoles:MediaRole[];mediaLibrary:MediaRecord[];onOpen:(t:Template)=>void}){
  const[topic,setTopic]=useState('')
  const[format,setFormat]=useState('')
  const[objective,setObjective]=useState('')
  const[materials,setMaterials]=useState('')
  const[duration,setDuration]=useState('')
  const[copied,setCopied]=useState(false)
  const[selectedVisualId,setSelectedVisualId]=useState('')
  const brief:CreativeBrief=useMemo(()=>({topic,format:format||undefined,objective:objective||undefined,materials:materials.split(',').map(x=>x.trim()).filter(Boolean),duration:duration?Number(duration):undefined}),[topic,format,objective,materials,duration])
  const plans=useMemo(()=>topic.trim()?buildProductionPlans({brief,domains,masters,storyboards,templates,assets,productionMasters,mediaRoles,mediaLibrary,limit:3}):[],[brief,domains,masters,storyboards,templates,assets,productionMasters,mediaRoles,mediaLibrary,topic])
  const primary=plans[0]
  const directions=useMemo(()=>directionsForMaster(primary?.master,visualMaps,visualArchitectures,format||undefined),[primary,visualMaps,visualArchitectures,format])
  const selectedVisual=directions.find(x=>x.architecture.id===selectedVisualId)||directions[0]
  const copyPlan=async()=>{if(!primary)return;await navigator.clipboard?.writeText(productionPrompt(primary,selectedVisual));setCopied(true);setTimeout(()=>setCopied(false),1200)}
  const runtime=primary?.runtime
  return <section className="briefWorkbench"><div className="briefWorkbench__intro"><span className="kicker">PRODUCTION PLANNER v0.8</span><h2>Del brief a una producción ejecutable.</h2><p>Elige Master, storyboard y dirección visual; luego resuelve composición Remotion y media factual sin inventar evidencia.</p></div><div className="briefWorkbench__form"><input value={topic} onChange={e=>setTopic(e.target.value)} placeholder="Tema: rehabilitación de pozo, rutas semanales, brigada…"/><div className="briefWorkbench__row"><select value={format} onChange={e=>setFormat(e.target.value)}><option value="">Formato automático</option><option value="reel">Reel</option><option value="carousel">Carrusel</option><option value="story">Story</option><option value="data">Datos</option><option value="presentation">Presentación</option></select><input value={objective} onChange={e=>setObjective(e.target.value)} placeholder="Objetivo: explicar beneficio…"/></div><div className="briefWorkbench__row"><input value={materials} onChange={e=>setMaterials(e.target.value)} placeholder="Material: 6 fotos, inversión, beneficiarios…"/><select value={duration} onChange={e=>setDuration(e.target.value)}><option value="">Duración automática</option><option value="15">15 s</option><option value="20">20 s</option><option value="30">30 s</option><option value="45">45 s</option><option value="60">60 s</option></select></div></div>{primary&&<div className="productionPlan"><div className="productionPlan__head"><div><small>MASTER RECOMENDADO</small><strong>{primary.master.id} · {primary.master.name}</strong></div><div className="readiness"><b>{primary.readiness}%</b><span>asset readiness</span></div><button onClick={copyPlan}>{copied?'Copiado':'Copiar prompt de producción'}</button></div>{runtime?.productionMaster&&<div className="runtimeBanner"><div><small>PRODUCTION MASTER EJECUTABLE</small><b>{runtime.productionMaster.id} · {runtime.productionMaster.name}</b><code>{runtime.compositionId}</code></div><div><b>{runtime.durationSeconds}s</b><span>duración objetivo</span></div><div><b>{runtime.mediaPlan?.readiness??0}%</b><span>media readiness</span></div></div>}<div className="productionPlan__visuals"><div className="productionPlan__sectionTitle"><small>3 DIRECCIONES VISUALES</small><span>elige arquitectura antes del template</span></div><div className="visualDirectionStrip">{directions.map(d=><VisualDirectionCard key={d.architecture.id} item={d} selected={selectedVisual?.architecture.id===d.architecture.id} onSelect={()=>setSelectedVisualId(d.architecture.id)}/>)}</div></div><div className="productionPlan__beats">{primary.storyboard.beats.map((b,i)=><div key={b.id}><b>{String(i+1).padStart(2,'0')}</b><span>{b.purpose}</span><small>{b.fields.join(' · ')}</small></div>)}</div>{[...primary.blockers,...(runtime?.mediaPlan?.blockers||[])].length>0&&<div className="productionPlan__blockers"><b>Antes de producción:</b>{[...primary.blockers,...(runtime?.mediaPlan?.blockers||[])].map(x=><span key={x}>{x}</span>)}</div>}<div className="productionPlan__templates">{primary.templates.slice(0,4).map(t=><button key={t.id} onClick={()=>onOpen(t)}><b>{t.id}</b><span>{t.name}</span></button>)}</div>{plans.length>1&&<div className="productionPlan__alts"><small>ALTERNATIVAS DE MASTER</small>{plans.slice(1).map(p=><span key={p.master.id}>{p.master.id} · {p.master.name} · {p.runtime?.productionMaster?.compositionId||'sin runtime'} · {p.readiness}%</span>)}</div>}</div>}</section>
}
