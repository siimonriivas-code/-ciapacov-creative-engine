import type {AssetRecord} from '../types'
export function AssetCard({asset}:{asset:AssetRecord}){return <article className="assetCard"><div className="assetCard__head"><code>{asset.id}</code><span>{asset.availability}</span></div><div className={`assetCard__thumb assetCard__thumb--${asset.availability==='bundled'?'ready':'slot'}`}>{asset.file?<img src={asset.file.replace(/^public\//,'/')} alt=""/>:<strong>SEMANTIC SLOT</strong>}</div><h3>{asset.name}</h3><p>{asset.notes||asset.capabilities.join(' · ')}</p><div className="chips">{asset.capabilities.map(c=><span key={c}>{c}</span>)}</div><small>{asset.source} · {asset.license.kind}</small></article>}
