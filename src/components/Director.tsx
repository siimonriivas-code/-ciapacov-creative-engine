import type { Template } from '../types'
import { routeTemplates } from '../lib/router'
export function Director({templates,query,setQuery,onOpen}:{templates:Template[];query:string;setQuery:(v:string)=>void;onOpen:(t:Template)=>void}){const picks=routeTemplates(templates,query,4);return <section className="director"><div><span className="kicker">CREATIVE DIRECTOR ROUTER</span><h1>Describe lo que necesitas.</h1><p>Busca primero en el catálogo; abre sólo los templates relevantes.</p></div><div className="director__search"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Ej. Reel para explicar una obra con cifras, fotos y recorrido…"/><div className="director__picks">{picks.map(t=><button key={t.id} onClick={()=>onOpen(t)}><b>{t.id}</b><span>{t.name}</span></button>)}</div></div></section>}
