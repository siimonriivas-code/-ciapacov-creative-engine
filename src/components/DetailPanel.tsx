import type { Template } from '../types'
export function DetailPanel({t,onClose}:{t:Template|null;onClose:()=>void}){if(!t)return null;const prompt=`Usa ${t.id} (${t.name}) como arquitectura base. Mantén el Design System activo como autoridad de marca. Adapta únicamente contenido y assets verificados; no uses demo data.`;const desc=t.description ?? `Arquitectura ${t.type} · ${t.subtype}.`;return <div className="backdrop" onClick={onClose}><aside className="detail" onClick={e=>e.stopPropagation()}><button className="detail__close" onClick={onClose}>×</button><code>{t.id}</code><h2>{t.name}</h2><p>{desc}</p><dl><div><dt>Formato</dt><dd>{t.format}</dd></div><div><dt>Pack</dt><dd>{t.pack}</dd></div><div><dt>Densidad</dt><dd>{t.density}</dd></div><div><dt>Motion</dt><dd>{t.motion}</dd></div><div><dt>Recipe</dt><dd>{t.recipe}</dd></div></dl><textarea readOnly value={prompt}/><button className="primary" onClick={()=>navigator.clipboard.writeText(prompt)}>Copiar prompt</button></aside></div>}
