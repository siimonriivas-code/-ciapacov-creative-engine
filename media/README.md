# Media Library

This directory contains only the **contract/examples** for large real-photo/video libraries. Do not dump production media into Git by default.

Use `index.example.json` as the metadata shape, then point to a controlled external storage location when the real Media Library is implemented.
